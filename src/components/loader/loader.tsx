import { Typography } from "@mui/material";
import React from "react";

const Loader = () => {
  return (
    <Typography
      sx={{
        margin: "50px",
        textAlign: "center",
        fontSize: "20px",
      }}
    >
      Loading.....
    </Typography>
  );
};

export default Loader;
